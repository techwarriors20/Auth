apiVersion: v1
kind: Service
metadata:
  name: {{ template "authapi.fullname" . }}
  labels:
    app: {{ template "authapi.name" . }}
    chart: {{ template "authapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "authapi.name" . }}
    release: {{ .Release.Name }}
